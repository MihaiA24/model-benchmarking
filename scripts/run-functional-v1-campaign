#!/usr/bin/env bash
set -euo pipefail

readonly PROJECT_ROOT=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
cd "$PROJECT_ROOT"
exec uv run --frozen python "$PROJECT_ROOT/scripts/run-functional-v1-campaign.py" "$@"
